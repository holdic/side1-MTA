-- Example:
-- ["user"] = {["version"] = ..., ...}

-- key with value nil ?
-- reserved keys?
-- only-readable?
-- writeable?
local service_hndl = {
	["using.user"] = "", ["user"] = {};
}
-- -> use the built-in ACL manager!!

--
local hold_protos = {
	
}

-- Checks if resources are loaded and run
-- Set strict resource-handling if needed (config)
function resource_subscribing()
	
end

-- Create requested keys if can
-- 
function resource_setProto()
	
end

function resource_setProtoFunction(method_type, method)
	
end

